# Get these from https://dashboard.plaid.com/developers/keys
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox

# Webhook URL — set this to your ngrok URL so Plaid can notify you when reports are ready.
# Run: ngrok http 8002
# Then paste the forwarding URL here — don't forget the /server/receive_webhook path at the end:
# WEBHOOK_URL=https://abc123.ngrok-free.app/server/receive_webhook
WEBHOOK_URL=
